<!doctype html>
<html lang="fi"><head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Muokattava Capra Combatin junioritoiminnan A4-esite ja PDF-tallennus.">
  <title>Capra Combat – junioriesitteen editori</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300..800&amp;family=Michroma&amp;display=swap" rel="stylesheet">
  <style>
    :root {
      --ui-bg: #e7e5de;
      --ui-panel: #f7f6f1;
      --ui-ink: #151611;
      --ui-muted: #686a62;
      --ui-line: #d0cec5;
      --ui-accent: #c8ae4d;
    }

    * { box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      background: var(--ui-bg);
      color: var(--ui-ink);
      font-family: "Inter", Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
    }

    button,
    input,
    select { font: inherit; }

    button { cursor: pointer; }

    .app-shell {
      min-height: 100vh;
      display: grid;
      grid-template-rows: auto 1fr;
    }

    .appbar {
      position: sticky;
      z-index: 50;
      top: 0;
      min-height: 70px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
      padding: 12px 22px;
      border-bottom: 1px solid #25261f;
      background: #10110e;
      color: #faf9f5;
      box-shadow: 0 8px 24px rgba(16, 17, 14, 0.14);
    }

    .app-brand {
      display: flex;
      align-items: center;
      gap: 13px;
      min-width: 0;
    }

    .app-brand img {
      width: 39px;
      height: 39px;
      object-fit: contain;
      filter: invert(1);
    }

    .app-brand-copy { min-width: 0; }

    .app-brand strong {
      display: block;
      overflow: hidden;
      font-size: 13px;
      letter-spacing: 0.08em;
      text-overflow: ellipsis;
      text-transform: uppercase;
      white-space: nowrap;
    }

    .app-brand span {
      display: block;
      margin-top: 3px;
      color: #a9aa9f;
      font-size: 11px;
    }

    .app-actions {
      display: flex;
      align-items: center;
      gap: 9px;
      flex-shrink: 0;
    }

    .ui-button {
      min-height: 42px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 0 16px;
      border: 1px solid #3c3e34;
      background: #1c1e18;
      color: #f7f6f1;
      font-size: 11px;
      font-weight: 800;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      transition: transform 160ms ease, background 160ms ease;
    }

    .ui-button:hover { transform: translateY(-1px); }
    .ui-button.primary { border-color: var(--ui-accent); background: var(--ui-accent); color: #10110e; }
    .ui-button.ghost { background: transparent; }
    .ui-button.danger { border-color: #75463f; color: #e9b3aa; }

    .editor-layout {
      min-width: 0;
      display: grid;
      grid-template-columns: 330px minmax(0, 1fr);
    }

    .sidebar {
      position: sticky;
      top: 70px;
      height: calc(100vh - 70px);
      overflow-y: auto;
      padding: 22px;
      border-right: 1px solid var(--ui-line);
      background: var(--ui-panel);
    }

    .sidebar h2 {
      margin: 0;
      font-size: 20px;
      letter-spacing: -0.035em;
    }

    .sidebar-intro {
      margin: 8px 0 20px;
      color: var(--ui-muted);
      font-size: 12px;
      line-height: 1.55;
    }

    .status-line {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 18px;
      color: #62645b;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .status-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: #68724a;
      box-shadow: 0 0 0 4px rgba(104, 114, 74, 0.12);
    }

    .control-group {
      margin-top: 12px;
      padding: 17px;
      border: 1px solid var(--ui-line);
      background: #fff;
    }

    .control-group h3 {
      margin: 0 0 15px;
      font-size: 10px;
      font-weight: 900;
      letter-spacing: 0.15em;
      text-transform: uppercase;
    }

    .field { display: grid; gap: 7px; margin-top: 13px; }
    .field:first-of-type { margin-top: 0; }

    .field label,
    .field-label {
      color: #5f6159;
      font-size: 10px;
      font-weight: 800;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    select,
    input[type="color"],
    input[type="range"],
    input[type="file"] { width: 100%; }

    select,
    input[type="file"] {
      min-height: 40px;
      padding: 8px 10px;
      border: 1px solid var(--ui-line);
      border-radius: 0;
      background: #f7f6f1;
      color: var(--ui-ink);
      font-size: 12px;
    }

    input[type="color"] {
      height: 40px;
      padding: 3px;
      border: 1px solid var(--ui-line);
      background: #f7f6f1;
    }

    input[type="range"] { accent-color: #68724a; }

    .range-row {
      display: grid;
      grid-template-columns: 1fr 44px;
      align-items: center;
      gap: 10px;
    }

    .range-value {
      color: #5f6159;
      font-size: 11px;
      font-weight: 800;
      text-align: right;
    }

    .toggle-list { display: grid; gap: 11px; }

    .toggle-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      color: #36382f;
      font-size: 12px;
      font-weight: 700;
    }

    .toggle-row input {
      width: 34px;
      height: 18px;
      accent-color: #68724a;
    }

    .stack-buttons { display: grid; gap: 8px; }

    .side-button {
      min-height: 40px;
      padding: 0 12px;
      border: 1px solid var(--ui-line);
      background: #f7f6f1;
      color: #22231e;
      font-size: 11px;
      font-weight: 800;
      letter-spacing: 0.05em;
      text-align: left;
      text-transform: uppercase;
    }

    .side-button:hover { background: #edeae1; }

    .photo-actions {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-top: 9px;
    }

    .help-card {
      margin-top: 12px;
      padding: 15px;
      background: #20221c;
      color: #f1efe8;
    }

    .help-card strong {
      display: block;
      color: #c8ae4d;
      font-size: 10px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    .help-card ol {
      margin: 10px 0 0;
      padding-left: 18px;
      color: #c7c7bf;
      font-size: 11px;
      line-height: 1.65;
    }

    .workspace {
      min-width: 0;
      overflow: auto;
      padding: 32px;
      background:
        linear-gradient(rgba(255,255,255,0.32) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.32) 1px, transparent 1px),
        #d7d5cd;
      background-size: 28px 28px;
    }

    .workspace-head {
      max-width: 210mm;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      margin: 0 auto 13px;
      color: #575950;
      font-size: 10px;
      font-weight: 800;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .edit-hint { display: inline-flex; align-items: center; gap: 8px; }
    .edit-hint::before { width: 8px; height: 8px; border: 1px dashed #68724a; content: ""; }

    .sheet-stage {
      width: max-content;
      min-width: 100%;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }

    /* A4-esite */
    .brochure {
      --paper: #f1efe8;
      --ink: #10110e;
      --muted: #606258;
      --secondary: #68724a;
      --secondary-light: #8a9567;
      --accent: #c8ae4d;
      --light: #faf9f5;
      --line: rgba(16, 17, 14, 0.16);
      --type-scale: 1;
      width: 210mm;
      min-height: 297mm;
      position: relative;
      isolation: isolate;
      overflow: hidden;
      flex: 0 0 auto;
      background: var(--paper);
      color: var(--ink);
      font-size: calc(10px * var(--type-scale));
      box-shadow: 0 22px 60px rgba(22, 23, 19, 0.24);
      transform: scale(var(--preview-zoom, 1));
      transform-origin: top center;
      transition: transform 180ms ease;
    }

    .brochure[data-theme="black"] {
      --paper: #eae7de;
      --ink: #0b0c0a;
      --muted: #55574f;
      --secondary: #2d3027;
      --secondary-light: #777d61;
      --accent: #c8ae4d;
    }

    .brochure[data-theme="light"] {
      --paper: #faf9f5;
      --ink: #161712;
      --muted: #606258;
      --secondary: #7d8761;
      --secondary-light: #96a173;
      --accent: #c8ae4d;
    }

    .brochure::after {
      position: absolute;
      z-index: -5;
      inset: 0;
      pointer-events: none;
      background-image:
        linear-gradient(rgba(16,17,14,0.026) 1px, transparent 1px),
        linear-gradient(90deg, rgba(16,17,14,0.026) 1px, transparent 1px);
      background-size: 18mm 18mm;
      content: "";
    }

    .flyer-header {
      min-height: 20mm;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10mm;
      padding: 5mm 12mm 4mm;
      background: var(--paper);
    }

    .flyer-brand {
      display: inline-flex;
      align-items: center;
      gap: 3mm;
    }

    .flyer-brand img {
      width: 11mm;
      height: 11mm;
      object-fit: contain;
    }

    .flyer-brand span { display: flex; flex-direction: column; line-height: 1; }

    .flyer-brand b {
      font-family: "Michroma", sans-serif;
      font-size: 1.22rem;
      font-weight: 400;
      letter-spacing: 0.18em;
    }

    .flyer-brand small {
      margin-top: 1.7mm;
      color: var(--secondary);
      font-family: "Michroma", sans-serif;
      font-size: 0.52rem;
      letter-spacing: 0.33em;
    }

    .header-label {
      padding-left: 7mm;
      border-left: 1mm solid var(--accent);
      font-size: 0.9rem;
      font-weight: 900;
   